# <topic> run<N>-<modelslug> — <一句话标题>

> 本模板为归档 README 的固定结构（中文交付）。`<>` 处替换为实际内容；
> 结构化元数据（模型/harness/协议/交付事实）按项目约定的格式存放，本
> README 只做叙述与展开，不要手抄一份会漂移的数字副本。

## 1. 效果图

![preview](preview.png)

`preview.png` 是全片信息密度最高的门面帧，注明其时点 `t = <s>s` 与画面
内容。来源命令：

```bash
ranim output <topic> --example <topic> <额外 features>
```

另附 2–5 张 capture，每张注明时点与内容：

- ![xxx](xxx.png) `t = <s>s`：<内容说明>

## 2. 原始 Prompt

逐字引用原始 prompt（位置遵循项目约定；不改动、不翻译），并注明有无
附件：

> <prompt 原文>

无附件。

## 3. 设计与实现思路

### 知识整理与叙事设计

叙事弧线（钩子 → … → 收尾）、全片时长、分幕结构（逐幕 beat 概述）。
受众假设与类比策略。

### 真实性实现

文件内置了哪些真实实现（模拟器/算法），时间线如何由其输出驱动，
`cargo test` 断言了哪些关键行为。**明确声明"屏幕上所有数字都是真的"
及实现方式。**

### 关键实现路径

结构模式（AnimStack / life_seq）、值得复用的工程决策、踩过的坑与规避。

## 4. 迭代过程（渲染 + 视觉检查记录）

逐轮记录（第 1 轮、第 2 轮……）：每轮渲染命令与耗时、抽帧时点、发现的
问题（逐条编号）、修复方式、复查结论。渲染 panic 也要如实记录。
最后一轮为终版验证（全片抽检 + 全部 capture 时点复核）。

## 5. 验证情况

- 构建：check / clippy / fmt 结果（含 features）
- 测试：用例数与断言内容
- 渲染：`ranim output` 成片参数（分辨率、时长、帧数）
- 视觉检查：累计抽检时点数与结论
- 环境备注：渲染环境的特殊情况

## 6. 环境

模型 / harness / 协议 / pin 等事实性信息按项目约定的结构化元数据存放，
此处只放指引，如有出入以元数据为准。
